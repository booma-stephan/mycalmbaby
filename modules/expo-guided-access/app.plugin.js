// This is a basic config plugin that doesn't modify the native project
// The native code is already set up in the ios directory
module.exports = function withExpoGuidedAccess(config) {
  return config;
};
